repos:
  - repo: https://github.com/astral-sh/ruff-pre-commit
    rev: v0.15.1
    hooks:
      # 1) Lint + fixes automáticos (incluye ordenar imports)
      - id: ruff
        args: [--fix]

      # 2) Formatter (decide single vs multiline imports)
      - id: ruff-format
